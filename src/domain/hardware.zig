const impl = @import("extensions/hardware.zig");

pub const HardwareHealthState = impl.HardwareHealthState;
pub const HardwareNode = impl.HardwareNode;
pub const HardwareRegistry = impl.HardwareRegistry;
