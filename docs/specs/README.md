# specs

## 当前分层

- `reference-aligned-ourclaw/`：当前 active spec
- `framework-based-ourclaw/`：跳转占位目录
- `backup/framework-based-ourclaw/`：phase-1 基线与已完成历史

## 使用规则

- 如果是继续推进 `ourclaw`，默认先读 `reference-aligned-ourclaw/`
- 如果要理解当前代码是如何从 phase-1 演进过来的，再读 `framework-based-ourclaw/`
- 不要把 phase-1 baseline 和 active spec 混成一个任务源
