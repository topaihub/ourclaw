# reference-aligned-ourclaw

> 当前 active spec（2026-03-16）。

## 目标

基于当前 `framework + ourclaw + ourclaw-manager` 已落地基线，参考：

- `nullclaw` 的 Zig 能力边界与运行时设计
- `openclaw` 的产品化控制面与工程治理语义

重新定义后续如何完成 `ourclaw`。

## 阅读顺序

1. `requirements.md`
2. `design.md`
3. `tasks.md`

## 与旧 spec 的关系

- `framework-based-ourclaw/`：已完成的 phase-1 基线
- `reference-aligned-ourclaw/`：当前唯一 active spec
